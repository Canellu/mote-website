# What crawlers receive is not necessarily what is written here. Cloudflare's
# managed robots.txt setting, when enabled on the zone, prepends its own
# Disallow groups for known AI crawlers ahead of this file, and a per-agent
# group beats the wildcard group below. That setting lives in the dashboard
# under Security > Settings > Bot traffic, so no commit can change it.
#
# Check the live file with `curl https://motedesktop.com/robots.txt` before
# concluding that the rules below are in effect. A blanket Disallow there blocks
# retrieval and citation as well as model training, which works against the
# AI-discoverability goal recorded in docs/discoverability.

User-agent: *
Allow: /

Sitemap: https://motedesktop.com/sitemap.xml
